MSTEST
*Blank solution
*Class library // crear una clase con metodos para luego testear
*new project MSTest type (name:projectname=al projecto a testear+MSTest)
*Packages que trae x default:
	-Microsoft.NET.Test.Sdk
	-MSTest.TestAdapter
	-MSTest.TestFramework
 * Crear una clase con metodos a testear (name= nombre de la clase cuyos 	metodos testeare+ MSTests-plural xq voy a tener muchos tests p/ esa 	class)
	ej:
  	[TestClass]
	public class CalculatorMSTests
	{
 	 [TestMethod]
	  public void AddNumbers_InputTwoNmbrs_OutputCorrectCalculation()
	  {
		//Arrange
		Calculator calc=new calc();
		//Act
		int result=calc.AddNumbers(2,2);
		//Assert
		Assert.AreEqual(4, result);
	  }
	}
 *AAA
 *Arrange (instanciar todas las dependencias)
 *Act(ejecutar metodo a testear)
 *Assert(comprobar resultados)
  * Assert.AreEqual(resultado esperado, resultado actual del metodo)
    Assert.That(resultado del metodo, AreEqual())
    Assert.IsFalse()
    Assert.IsTrue

Test/TestExplorer/RunTests


